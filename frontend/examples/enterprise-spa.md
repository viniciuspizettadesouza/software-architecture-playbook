# Example — Larger / Enterprise SPA

Possible starting blueprint:

```text
React + TypeScript + Vite
        ↓
feature-oriented modules
        ↓
React Router
        ↓
service/API boundaries
        ↓
TanStack Query for server state
        ↓
Zod at runtime boundaries
        ↓
Vitest + Testing Library
        ↓
Playwright for critical journeys
        ↓
CI: format/lint/typecheck/test/build/E2E
```

Add contract generation when frontend/backend drift becomes a real risk. Add a component library/design system according to product/design constraints.
