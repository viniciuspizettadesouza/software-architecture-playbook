# ESLint

**Label:** Default

Use ESLint to automate code-quality and project conventions that TypeScript does not cover.

Prefer a focused ruleset that catches real defects and architectural drift. Avoid rule collections that generate high-noise churn without measurable value.
